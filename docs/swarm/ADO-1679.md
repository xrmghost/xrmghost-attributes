# ADO-1679 — xrmghost-attributes NuGet metadata + README prep

## Goal

Prepare xrmghost-attributes for the next NuGet release by fixing the package's
public metadata target (`PackageProjectUrl`) and reshaping the dual-use
README so it exposes package-appropriate first-party backlinks, without
changing any runtime behavior.

## Scope and modifications

- `XrmGhost.Attributes.csproj`: changed `PackageProjectUrl` from
  `https://github.com/xrmghost/xrmghost-attributes` to
  `https://docs.xrmghost.tech/attributes/` (the approved docs destination for
  Attributes). `RepositoryUrl` and `PackageReadmeFile` left untouched.
- `README.md`:
  - Differentiated the opening link block: **Documentation** now points to
    the Attributes-specific docs page (`/attributes/`) instead of the bare
    docs homepage; added a dedicated **Source** link to the GitHub repo;
    kept **Releases** pointing at GitHub Releases for this repo. This avoids
    the package page reading as a generic duplicate of another XrmGhost
    public surface.
  - Added an explicit "Package metadata note" callout stating that the
    corrected metadata is only observable on a newly published NuGet
    version, and that historical versions already on nuget.org cannot be
    changed retroactively.
- No `.cs` files, no workflow files, no `CHANGELOG.md`, no test files were
  touched.

## Commands executed

- `rg -n "PackageProjectUrl|PackageReadmeFile|RepositoryUrl" XrmGhost.Attributes.csproj README.md`
- `git diff -- README.md XrmGhost.Attributes.csproj`
- `dotnet restore xrmghost-attributes.sln` → restore OK.
- `dotnet build xrmghost-attributes.sln -c Release --no-restore` → build
  succeeded, 0 warnings, 0 errors; nupkg
  `bin/Release/XrmGhost.Attributes.0.6.27.nupkg` produced.
- `dotnet test xrmghost-attributes.sln --no-build --configuration Release` →
  15/15 tests passed.
- `rg -n "https://docs\.xrmghost\.tech/attributes/|newly published|historical versions" README.md XrmGhost.Attributes.csproj`
  → all three markers present.
- `git diff --name-only | rg "^(README\.md|XrmGhost\.Attributes\.csproj)$"` →
  confirmed diff is limited to exactly these two files.

## Acceptance criteria verified

- [x] `XrmGhost.Attributes.csproj` sets `PackageProjectUrl` to
      `https://docs.xrmghost.tech/attributes/`.
- [x] `README.md` contains package-appropriate first-party backlinks/copy
      (differentiated Documentation/Source/Releases links) that remain valid
      on both GitHub and NuGet renders, and are not a generic duplicate of
      another XrmGhost public surface.
- [x] The public package surface explicitly states the metadata fix is only
      observable on a newly published NuGet version and does not
      retroactively alter old package versions.
- [x] No runtime contracts, library source files (`.cs`), or release
      workflow mechanics were changed.

## Residual risks

- The README callout is prose-only; there is no automated guard preventing
  a future edit from silently dropping the "newly published version" caveat
  again. Acceptable given the task's scope constraint (README + csproj
  only).
- `PackageProjectUrl` now points to a docs page that must exist and stay
  live at `https://docs.xrmghost.tech/attributes/`; this is an external
  dependency outside this repo's control.

## Notes for review

- Diff is strictly limited to `README.md` and `XrmGhost.Attributes.csproj`
  as required by `files_to_edit`.
- `files_not_to_touch` (`.github/workflows/*`, `XrmGhost.Attributes.Tests/**`,
  `**/*.cs`, `CHANGELOG.md`) were not modified — verified via `git status`
  and the final `git diff --name-only` check.
- Build and full test suite pass with the changes in place; the change is
  metadata/documentation-only and does not affect assembly output beyond the
  package manifest fields.
